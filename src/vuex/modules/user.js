import * as types from '../types'
export default {
	state: {
		show: false
	},
	mutations: {
		switch(state){
			state.show == state.show?false:true;
		}
	}
//	,
//	getters: {
//		not_show(state){
//			return !state.show;
//		}
//	}
}
